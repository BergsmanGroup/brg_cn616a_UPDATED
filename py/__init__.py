"""
Top-level package for CN616A modules.

The `py` directory is treated as a Python package so that
scripts under it can be executed with `-m` and perform relatives
imports cleanly.
"""

__version__ = "0.1.0"
